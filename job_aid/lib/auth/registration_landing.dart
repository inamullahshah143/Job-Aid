import 'package:flutter/material.dart';

class RegistrationLanding extends StatelessWidget {
  const RegistrationLanding({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
